2021-01-22

Today i have been watch all the basic php concept video as per today task and practice on that which is mentioned in practice folder

1) in echo and print method of show output i learn that echo has no return value while print has a return value of 1 so it can be used in expressions.

2) in error and warning i have learn that , warning doesn't stop execution it will show remaining output where error will stop execution and show error

3) in variable define i learn that using ($) sign. we can define variable in php file , php variable is dynamic(in terms of data type) , means we don't need to specify type of variable it automatically take itself from data.

4) at the end pratice on all given topic by today task like loop's switch statment , concatination ,embedding php inside html etc.